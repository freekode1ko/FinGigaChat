name: CI_service

on:
  workflow_call:
    inputs:
      service_path:
        required: true
        type: 'string'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - id: greeting
      run: echo "${{ inputs.name }}"